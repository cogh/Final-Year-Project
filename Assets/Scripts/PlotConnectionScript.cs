using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlotConnectionScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public GameObject entranceNode;
    public GameObject exitNode;
    public GameObject parentPlot;
    public GameObject targetPlot;
}
